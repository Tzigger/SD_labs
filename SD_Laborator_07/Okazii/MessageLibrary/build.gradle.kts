// MessageLibrary — shared data model, HeartBeatClient, MetricsCollector
// No shadow JAR needed; other modules depend on it via project(:MessageLibrary)
dependencies {
    implementation(kotlin("stdlib"))
}
